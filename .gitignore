# build
target/
*.class
dist/
# collector binaries fetched in CI/itest
otelcol-contrib
*.tar.gz
# session tooling / local
.remember/
.idea/
*.iml
# throwaway Phase 0 probe (rollback = delete probe/)
probe/

# itest local/heavy artifacts (not committed)
itest/otel/*.jar
itest/out/
itest/initdb/err.log
